/**
 * Convert Radians to Degrees
 *
 * @since 1.0.0
 * @category Convert
 * @param {Number} degrees
 * @returns {Number}
 */
function convRadToDeg(radians) {
  return (radians * 180) / Math.PI;
}

export default convRadToDeg;
